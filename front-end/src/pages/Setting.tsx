import React from 'react';
import classNames from 'classnames/bind';
import styles from './Setting.scss';

const cx = classNames.bind(styles);

interface IOwnProps {
};

const Setting: React.FC<IOwnProps> = ({

}) => {
    return (
        <div>Setting</div>
    );
};

export default Setting;