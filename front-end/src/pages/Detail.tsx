import React from 'react';
import classNames from 'classnames/bind';
import styles from './Detail.scss';

const cx = classNames.bind(styles);

interface IOwnProps {
};

const Detail: React.FC<IOwnProps> = ({

}) => {
  return (
    <div>Detail</div>
  );
};

export default Detail;
